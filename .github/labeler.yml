# Conventional Commit Labels
# https://www.conventionalcommits.org/en/v1.0.0/
feat:
    - '(feat)'
fix:
    - '(fix)'
chore:
    - '(chore)'
docs:
    - '(docs)'
refactor:
    - '(refactor)'
style:
    - '(style)'
perf:
    - '(perf)'
test:
    - '(test)'

# Label configuration for issue labeler
scopes: []  # Initial empty array for scopes

# Add any other label configurations below
# Example:
# bug:
#   - '(bug|fix|defect)'

